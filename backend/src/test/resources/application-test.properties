# ================================
# Test Configuration - Docker Compose PostgreSQL
# ================================
# Usa il database PostgreSQL avviato da docker-compose (localhost:5432)
spring.datasource.url=jdbc:postgresql://localhost:5432/tennis_club
spring.datasource.username=tennis_admin
spring.datasource.password=br0do27_
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway per applicare migrazioni
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Disabilita notifiche email nei test
spring.mail.host=localhost
spring.mail.port=25
app.notifications.enabled=false
